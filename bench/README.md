# bench/

Benchmark scripts and results. `results/` holds committed shots/sec and
latency numbers (Phase 4+), each tagged with machine, clock, batch size,
and whether host setup time is included — see the "Working rules" section
of the project brief for why that discipline matters.
