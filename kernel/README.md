# kernel/

HLS C++ kernel sources: `stim_frame_sampler.cpp`, `gate_ops.hpp`,
`frame_store.hpp`, `prng.hpp`, `detector_fold.hpp`, and per-module C-sim
testbenches under `hls_testbench/`.

Empty as of Phase 0 by design — see the phased plan in the top-level
[README](../README.md). Populated starting Phase 2.
