"""stim-u55c: FPGA-accelerated, Stim-compatible bulk sampling for
stabilizer error-correction circuits on the Alveo U55C.

The Stim-API-compatible sampler (CompiledDetectorSampler), the PyMatching
2 decode integration, and the sinter backend land in Phase 5 once there is
hardware output to wrap. This package is an empty placeholder until then.
"""

__version__ = "0.0.0.dev0"
